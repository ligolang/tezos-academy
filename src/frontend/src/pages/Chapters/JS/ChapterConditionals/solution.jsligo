type ship_code = string;
let my_ship: ship_code = "020433";
my_ship = "222031";
const my_ship_price : tez = 3tez * 120n;

const modify_ship = (my_ship: ship_code): ship_code => {
    // Type your solution below
    if (String.sub(2n, 1n, my_ship) == "0") {
        return String.sub(0n, 2n, my_ship) + "1" + String.sub(3n, 3n, my_ship);
    } else {
        return my_ship;
    }
}
